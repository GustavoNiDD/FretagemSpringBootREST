name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up JDK 17 and Docker
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt'
        java-version: '17'

    - name: Debug Project Structure
      run: |
        ls -R  # Lista o conteúdo do diretório
        ls -l target  # Verifica as permissões do diretório target

    - name: Build Docker Image
      run: docker build -t gustavonid/fretes_infinity:latest .
      working-directory: path/to/your/project/directory

    - name: Log in to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker Image to Registry
      run: docker push gustavonid/fretes_infinity:latest
